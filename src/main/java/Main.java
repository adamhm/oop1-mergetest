
public class Main {
    public static void main(String[] args) {
        Range range1 = new Range(4);
        System.out.printf("Start, end, length: %s, %s, %s%n", range1.getStart(), range1.getEnd(), range1.length());
        Range range2 = new Range(2);
        System.out.printf("Start, end, length: %s, %s, %s%n", range2.getStart(), range2.getEnd(), range2.length());
        
    }
}
